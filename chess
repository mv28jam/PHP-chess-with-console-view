#!/usr/bin/env php
<?php
//check for version
if((int)substr(str_replace('.','', phpversion()), 0, 2) < 71){
    die('PHP version have to be 7.2 or higher.'.PHP_EOL);
}

//сomposer
require_once (__DIR__ . '/vendor/autoload.php');

//load
foreach (glob('src/*.php') as $file) {
    require_once $file;
}

//
$game = new Game();

